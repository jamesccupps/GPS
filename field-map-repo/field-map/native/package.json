{
  "name": "field-map-native",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper around the built field map. Exists for background location: Android suspends a web page when the screen sleeps, which stops a recorded track.",
  "scripts": {
    "www": "python3 scripts/assemble.py",
    "sync": "python3 scripts/assemble.py && cap sync android",
    "apk": "python3 scripts/assemble.py && cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor-community/background-geolocation": "^1.2.26",
    "@capacitor/android": "^7.6.8",
    "@capacitor/core": "^7.6.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.8"
  }
}
